{
  "name": "brain-games",
  "version": "1.0.0",
  "description": "Brain Games — منصة ألعاب عقل متعددة (Number Match + Sudoku)",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf www && mkdir -p www && cp -r index.html styles js assets www/",
    "sync": "npm run build && npx cap sync",
    "ios": "npm run build && npx cap sync ios",
    "android": "npm run build && npx cap sync android",
    "open:ios": "npx cap open ios",
    "open:android": "npx cap open android",
    "ota": "bash deploy-ota.sh",
    "ota:patch": "bash deploy-ota.sh"
  },
  "dependencies": {
    "@capacitor-community/admob": "^8.1.0",
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/ios": "^8.5.0",
    "@capgo/capacitor-updater": "^8.51.14",
    "cordova-plugin-purchase": "^13.18.0",
    "sharp": "^0.35.3"
  }
}
